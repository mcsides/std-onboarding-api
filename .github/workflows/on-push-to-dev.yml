name: On push to dev

on:
  push:
    branches:
      - dev

permissions:
  id-token: write
  contents: write

jobs:
  build:
    name: Build
    uses: Stamper-Labs/core-flows/.github/workflows/yarn-nestjs-build.yml@dev
    secrets: inherit

  dockerize:
    needs: build
    name: Dockerize
    uses: Stamper-Labs/core-flows/.github/workflows/yarn-nestjs-dockerize.yml@dev

  deploy-to-staging:
    needs: dockerize
    name: Deploy to Staging
    uses: Stamper-Labs/core-flows/.github/workflows/yarn-nestjs-deploy.yml@dev
    secrets: inherit