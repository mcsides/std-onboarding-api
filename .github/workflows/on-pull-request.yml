name: On Pull Request

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened
      - edited

jobs:
  build-onboarding-api:
    name: Build Onboarding API
    uses: Stamper-Labs/core-flows/.github/workflows/yarn-nestjs-build.yml@release-from-main
    with:
      node-version: "22"
      deploy-mode: false
    secrets: inherit